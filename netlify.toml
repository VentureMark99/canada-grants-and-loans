[build]
  command   = "npm run build"
  publish   = "out"

[build.environment]
  NODE_VERSION = "20"

# Serve Next.js static export 404 page
[[redirects]]
  from   = "/*"
  to     = "/404.html"
  status = 404
